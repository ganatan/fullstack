## Commandes

- push  
  `git push -u -f origin main`

  pousse ta branche locale `main` vers `origin/main`  
  `-u` : lie `main` à `origin/main` (ensuite `git push` suffit)  
  `-f` : force l’écrasement de `origin/main` avec ton historique local