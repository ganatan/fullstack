# IntelliJ IDEA — Raccourcis


## Raccourcis Commentaires

Chemin :

```text
File > Settings > Keymap
Main Menu > Code > 
Taper en recherche Comment
```

Ensuite :

```text
Remove les keys existants sur les deux comment with line comment
Remove les keys existants sur les deux comment with block comment
```

```text
Clic droit sur Comment with line Comment  > Add Keyboard Shortcut
```
Puis appuyer sur :

```text
Ctrl + :
```

Valider avec :

```text
OK
```

---

## Raccourci Lancement du Terminal

Raccourci :

```
Alt + F12
```

View → Tool Windows → Terminal

## Raccourcis Formatage du code

Chemin :

```text
File > Settings > Keymap
Main Menu > Code > 
Taper en recherche Reformat Code
```

Ensuite :

```text
Remove les keys existants
```

```text
Clic droit sur Reformat Code > Add Keyboard Shortcut
```
Puis appuyer sur :

```text
Alt + Maj + F
```

Valider avec :

```text
OK
```

Si IntelliJ signale un conflit :
- choisir `Remove` pour affecter ce raccourci au formatage
- ou choisir `Leave` pour conserver les deux raccourcis

Nom exact de l’action :

```text
Reformat Code
```

## Raccourcis Indentation

### Java

Editor → Code Style → Java → Tabs and Indents  

- Indent : 2
- Tab size : 2

### XML

Editor → Code Style → XML  

Indentation identique

---

## Fermer un onglet dans IntelliJ

Chemin :

```text
File > Settings > Keymap
Main Menu > Window > Editor Tabs > Close
```

Ensuite :

```text
Clic droit sur Close > Add Keyboard Shortcut
```

Puis appuyer sur :

```text
Ctrl + W
```

Valider avec :

```text
OK
```

Attention :
`Ctrl + W` est souvent déjà utilisé pour une autre action.

Si IntelliJ signale un conflit :
- choisir `Remove` pour affecter ce raccourci à la fermeture de l’onglet
- ou choisir `Leave` pour conserver les deux raccourcis

Nom exact de l’action :

```text
Close
```